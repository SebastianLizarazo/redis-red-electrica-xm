# Tests unit: fakes para Redis (fakeredis) y XM (respx); sin red.
# Pytest en modo asyncio auto. Strict TDD está OFF por decisión del equipo
# (memoria sdd-init), pero los tests de estos módulos siguen siendo
# recomendados para evitar regresiones silenciosas.
