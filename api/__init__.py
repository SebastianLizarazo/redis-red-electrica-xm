# Backend HTTP + SSE con FastAPI: expone /api/health, /api/state,
# /api/metrics, /api/alerts, /api/events (replay), /api/stream (SSE),
# /api/stress/{event} (inyección de eventos anómalos).
# Responsabilidad de Sebastián.
